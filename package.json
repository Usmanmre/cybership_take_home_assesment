{
  "name": "carrier-integration-service",
  "version": "1.0.0",
  "description": "Shipping carrier integration service — UPS Rating API wrapper with extensible carrier/operation design",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "example": "node scripts/example.mjs",
    "run": "npm run build && npm run example",
    "live": "node scripts/run-live.mjs"
  },
  "keywords": ["shipping", "carrier", "ups", "rates"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "zod": "^3.22.0"
  }
}
