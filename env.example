# Carrier Integration Service — required environment variables
# Copy this file to .env and fill in values. Do not commit .env.
# Usage: cp env.example .env

# UPS API (required for UPS carrier)
UPS_CLIENT_ID=
UPS_CLIENT_SECRET=

# UPS API URLs (defaults; override for sandbox/production)
UPS_API_BASE_URL=https://onlinetools.ups.com
UPS_OAUTH_TOKEN_URL=https://onlinetools.ups.com/security/v1/oauth/token

# Optional
HTTP_TIMEOUT_MS=15000
OAUTH_TIMEOUT_MS=10000
OAUTH_REFRESH_BUFFER_SECONDS=60
